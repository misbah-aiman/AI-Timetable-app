{
  "name": "ai-timetable",
  "version": "1.0.0",
  "description": "A full-stack AI-powered timetable web app that generates personalized daily/weekly schedules using OpenAI GPT, with time tracking, analytics, and a modern SaaS-style UI.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
